[User Interface]
webui_web_engine = firefox
# Keep account creation and date/time in the installer. There is no separate
# GNOME initial-setup dependency in this Hyprland image.
hidden_webui_pages =
hidden_spokes =

[Storage]
default_scheme = BTRFS

[Network]
default_on_boot = FIRST_WIRED_WITH_LINK
