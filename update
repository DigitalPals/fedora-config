#!/usr/bin/env bash
# Repository entry point for the durable updater shared with Quickshell.
set -euo pipefail

repo_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec "$repo_dir/assets/scripts/xps-update-run" run --repo "$repo_dir" "$@"
