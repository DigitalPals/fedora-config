[Unit]
Description=Quickshell desktop shell
PartOf=hyprland-session.target

[Service]
Type=simple
ExecStart={{ primary_home }}/.local/bin/fedora-config-runtime exec quickshell
Environment=PATH={{ primary_home }}/.local/bin:{{ primary_home }}/.npm-global/bin:/usr/local/bin:/usr/bin
Environment=FEDORA_CONFIG_CONNECTED_WIDGETS={{ (features.connected_widgets | bool) | ternary('1', '0') }}
Restart=on-failure
RestartSec=2

[Install]
WantedBy=hyprland-session.target
